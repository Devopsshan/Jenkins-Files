pipeline { 
    agent any 
    stages { 
        stage("Checkout /Clone from Git & Maven Build") { 
            steps { 
                      git branch: "Main" ,   url : "https://github.com/Devopsshan/Jenkins-Files.git"
                      sh  'mvn clean'
                      sh  ' mvn package'
            } 
        } 
